services:
  app:
    build:
      context: .          
      dockerfile: Dockerfile
    container_name: stock-alert-app
    env_file:
      - .env.prod
    ports:
      - "8888:3000"
    restart: unless-stopped